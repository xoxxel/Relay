# مستند فنی پروژه: LAN Share
### اپلیکیشن دسکتاپ اشتراک‌گذاری فایل و متن در شبکه خانگی

> این سند برای این نوشته شده که یک دستیار هوش مصنوعی (یا توسعه‌دهنده) بتونه بدون نیاز به سوال اضافه، این پروژه رو از صفر پیاده‌سازی کنه. همه‌ی تصمیمات معماری، طراحی و API از قبل مشخص شده. هر بخش که نیاز به تصمیم بیشتر داشته باشه، به‌صورت صریح به‌عنوان "TODO برای پیاده‌سازی" علامت‌گذاری شده.

---

## ۱. معرفی و هدف پروژه

یک اپلیکیشن دسکتاپ بومی برای **Linux Mint** که:
- یک سرور HTTP محلی روی شبکه‌ی خانگی (LAN) راه‌اندازی می‌کند.
- کاربر با یک **دکمه toggle** سرور را روشن/خاموش می‌کند.
- سایر دیوایس‌های خانه (عمدتاً موبایل‌های اندرویدی) **بدون نصب هیچ برنامه‌ای**، فقط با باز کردن یک آدرس در مرورگر (یا اسکن QR Code)، می‌توانند:
  - فایل آپلود/دانلود کنند و در پوشه‌های اشتراکی مرور کنند.
  - متن به یک **کلیپ‌بورد مشترک زنده** ارسال کنند که فوراً روی همه‌ی دیوایس‌های متصل (از جمله خود دسکتاپ) ظاهر می‌شود.

### محدودیت‌های سخت (غیرقابل‌مذاکره)
1. هیچ نرم‌افزار/اپلیکیشنی روی دیوایس‌های دیگر نصب نمی‌شود؛ دسترسی فقط از طریق مرورگر وب استاندارد (Chrome/Firefox موبایل).
2. سیستم میزبان: Linux Mint (دبیان‌بیس، دسکتاپ Cinnamon).
3. ظاهر باید **مینیمال، جذاب و حرفه‌ای** باشد؛ نه یک قالب عمومی SaaS.
4. بدون رمز عبور — فرض بر امن‌بودن شبکه‌ی خانگی است (ولی معماری باید طوری باشد که افزودن PIN در آینده ساده باشد).
5. فیچر کلیپ‌بورد زنده باید از همان نسخه‌ی اول وجود داشته باشد.

---

## ۲. معماری کلی

```
┌─────────────────────────────────────────────┐
│              Tauri Desktop App                │
│  ┌─────────────────────────────────────────┐  │
│  │   Vue 3 Control Panel (WebView)          │  │
│  │   - Toggle سرور                          │  │
│  │   - نمایش QR / آدرس                       │  │
│  │   - انتخاب پوشه اشتراکی                    │  │
│  │   - تب کلیپ‌بورد زنده                      │  │
│  └───────────────┬───────────────────────────┘  │
│                  │ Tauri invoke (IPC)            │
│  ┌───────────────▼───────────────────────────┐  │
│  │   Rust Core (src-tauri)                    │  │
│  │   - start_server() / stop_server()         │  │
│  │   - axum HTTP Server (async task جدا)      │  │
│  │   - mDNS broadcaster                       │  │
│  │   - SQLite (تاریخچه کلیپ‌بورد)              │  │
│  └───────────────┬───────────────────────────┘  │
└──────────────────┼────────────────────────────┘
                    │  LAN  (http://192.168.x.x:8420)
      ┌─────────────┼─────────────┐
      ▼             ▼             ▼
  📱 موبایل ۱    📱 موبایل ۲    💻 لپ‌تاپ دیگر
  (مرورگر فقط)   (مرورگر فقط)   (مرورگر فقط)
      ▲
      │ همون سرور axum یک باندل جدای Vue
      │ رو هم به این دیوایس‌ها سرو می‌کنه (web-client)
```

نکته‌ی کلیدی: **دو باندل Vue جدا** ساخته می‌شود:
- `desktop-panel`: داخل پنجره‌ی Tauri اجرا می‌شود (فقط روی سیستم خود کاربر).
- `web-client`: توسط سرور axum سرو می‌شود و همان چیزی است که روی موبایل باز می‌شود. این باندل با `rust-embed` داخل باینری نهایی Rust جاسازی می‌شود تا هیچ فایل جانبی لازم نباشد.

---

## ۳. پشته فناوری (Tech Stack)

### Backend (src-tauri, زبان Rust)
| بسته | نقش |
|---|---|
| `tauri` (v2) | Shell دسکتاپ، پنجره، Tray icon |
| `axum` | سرور HTTP + WebSocket |
| `tokio` (features = ["full"]) | Async runtime |
| `tower-http` (features = ["cors", "fs"]) | CORS و سرو فایل استاتیک |
| `serde`, `serde_json` | سریالایز/دیسریالایز JSON |
| `rusqlite` (features = ["bundled"]) | ذخیره‌سازی تاریخچه کلیپ‌بورد |
| `mdns-sd` | تبلیغ آدرس روی شبکه (`.local`) |
| `local-ip-address` | گرفتن IP فعلی سیستم در شبکه‌ی محلی |
| `uuid` (features = ["v4"]) | شناسه‌ی یکتا برای آیتم‌های کلیپ‌بورد و فایل‌ها |
| `chrono` | Timestamp |
| `rust-embed` | جاسازی build خروجی `web-client` داخل باینری |
| `tauri-plugin-store` | ذخیره‌ی تنظیمات (مسیر پوشه اشتراکی، پورت) |
| `tauri-plugin-clipboard-manager` | کپی مستقیم آیتم کلیپ‌بورد به کلیپ‌بورد سیستم دسکتاپ |

### Frontend (هر دو باندل)
| بسته | نقش |
|---|---|
| Vue 3 + Vite | فریمورک اصلی |
| Tailwind CSS | استایل‌دهی |
| `qrcode.vue` | تولید QR Code سمت کلاینت از روی آدرس |
| `pinia` | مدیریت state (وضعیت سرور، لیست کلیپ‌بورد) |
| `@vueuse/core` | composable های کمکی (useWebSocket و ...) |
| `lucide-vue-next` | آیکون‌های خطی مینیمال |

---

## ۴. ساختار پوشه‌ها

```
lan-share/
├── src-tauri/
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   ├── icons/
│   └── src/
│       ├── main.rs
│       ├── commands.rs        # توابع invoke شده از Vue
│       ├── server/
│       │   ├── mod.rs
│       │   ├── routes_files.rs
│       │   ├── routes_clip.rs
│       │   └── ws.rs
│       ├── mdns.rs
│       ├── db.rs               # SQLite برای کلیپ‌بورد
│       ├── state.rs             # AppState مشترک (Arc<Mutex<...>>)
│       └── web_assets.rs        # rust-embed برای web-client/dist
├── desktop-panel/               # Vue app پنل دسکتاپ
│   ├── src/
│   │   ├── App.vue
│   │   ├── stores/serverStore.js
│   │   └── components/
│   │       ├── ServerToggle.vue
│   │       ├── ConnectionCard.vue     # QR + آدرس
│   │       ├── FolderPicker.vue
│   │       ├── ClipboardPanel.vue
│   │       └── ActivityFeed.vue
│   ├── tailwind.config.js
│   └── vite.config.js
└── web-client/                  # Vue app که روی موبایل باز می‌شه
    ├── src/
    │   ├── App.vue
    │   ├── views/
    │   │   ├── FilesView.vue
    │   │   └── ClipboardView.vue
    │   └── components/
    │       ├── BreadcrumbBar.vue
    │       ├── FileRow.vue
    │       ├── UploadFab.vue
    │       └── ClipComposer.vue
    ├── tailwind.config.js
    └── vite.config.js
```

---

## ۵. مدل داده‌ها

### FileItem (پاسخ لیست فایل‌ها)
```json
{
  "name": "Vacation.jpg",
  "path": "Photos/Vacation.jpg",
  "is_dir": false,
  "size_bytes": 2431231,
  "modified_at": "2026-09-01T14:22:00Z",
  "mime_type": "image/jpeg"
}
```

### ClipItem (آیتم کلیپ‌بورد)
```json
{
  "id": "a1b2c3d4",
  "content": "متن ارسال‌شده",
  "device_label": "iPhone موبایل آشپزخانه",
  "created_at": "2026-09-05T10:12:00Z"
}
```

### ServerStatus (وضعیت سرور، برگردانده‌شده از Tauri command)
```json
{
  "running": true,
  "port": 8420,
  "lan_ip": "192.168.1.23",
  "lan_url": "http://192.168.1.23:8420",
  "mdns_hostname": "lanshare.local",
  "shared_folder": "/home/user/Shared",
  "connected_clients": 2
}
```

---

## ۶. مشخصات کامل REST API

سرور axum روی پورت پیش‌فرض `8420` (قابل تغییر از تنظیمات) بالا می‌آید. مسیر پایه: `/api`.

| Method | مسیر | توضیح | Body / Query |
|---|---|---|---|
| GET | `/api/files?path=` | لیست فایل‌ها و پوشه‌های یک مسیر (نسبی به ریشه اشتراکی) | query: `path` (پیش‌فرض `""` یعنی ریشه) |
| POST | `/api/files/upload` | آپلود یک یا چند فایل | multipart/form-data، فیلد `path` (مقصد) + فیلدهای فایل |
| GET | `/api/files/download?path=` | دانلود یک فایل مشخص (stream) | query: `path` |
| POST | `/api/files/mkdir` | ساخت پوشه جدید | json: `{ "path": "Photos/New Folder" }` |
| DELETE | `/api/files?path=` | حذف فایل یا پوشه (بازگشتی) | query: `path` |
| GET | `/api/clips` | تاریخچه‌ی ۵۰ آیتم آخر کلیپ‌بورد | - |
| POST | `/api/clips` | ارسال متن جدید به کلیپ‌بورد مشترک | json: `{ "content": "...", "device_label": "..." }` |
| DELETE | `/api/clips/:id` | حذف یک آیتم کلیپ‌بورد | - |
| GET | `/ws` | اتصال WebSocket برای دریافت رویدادهای زنده | - |

### قوانین مسیرها (Path Safety)
- تمام مسیرهای ورودی باید نسبت به `shared_folder` نرمالایز و بررسی شوند تا از **Path Traversal** (مثل `../../etc/passwd`) جلوگیری شود. هر درخواستی که بعد از canonicalize خارج از ریشه‌ی اشتراکی بیفتد، باید با کد `403` رد شود.

### پیام‌های WebSocket (JSON، فیلد `type` مشخص‌کننده نوع رویداد)
```json
{ "type": "clip_added", "data": { /* ClipItem */ } }
{ "type": "clip_removed", "data": { "id": "a1b2c3d4" } }
{ "type": "file_added", "data": { /* FileItem */ } }
{ "type": "file_removed", "data": { "path": "Photos/old.png" } }
```
هر دو باندل (پنل دسکتاپ و web-client) به این WebSocket وصل می‌شوند تا لیست فایل‌ها و کلیپ‌بورد بدون رفرش صفحه به‌روز بماند.

---

## ۷. توابع Tauri (IPC Commands)

| نام Command | ورودی | خروجی | کار |
|---|---|---|---|
| `start_server` | `{ port?: number }` | `ServerStatus` | راه‌اندازی axum + mDNS روی thread جدا |
| `stop_server` | - | `ServerStatus` | خاموش‌کردن graceful سرور و mDNS |
| `get_server_status` | - | `ServerStatus` | وضعیت فعلی |
| `pick_shared_folder` | - | `{ path: string }` | باز کردن دیالوگ انتخاب پوشه بومی (native file dialog) |
| `copy_to_system_clipboard` | `{ text: string }` | - | کپی متن به کلیپ‌بورد خود سیستم مینت |
| `get_settings` / `save_settings` | - / `Settings` | `Settings` | خواندن/نوشتن تنظیمات (پورت، پوشه، نام دستگاه) |

---

## ۸. کشف آدرس (Discovery) بدون تایپ دستی

دو راه هم‌زمان (کاربر هرکدام راحت‌تر بود استفاده می‌کند):

1. **QR Code**: به‌محض روشن‌شدن سرور، `lan_url` از Rust به Vue برمی‌گردد و با `qrcode.vue` رندر می‌شود. کاربر با دوربین موبایل اسکن می‌کند.
2. **mDNS**: با کرِیت `mdns-sd`، نام میزبان (مثلاً `lanshare.local`) روی شبکه تبلیغ می‌شود تا در صورت پشتیبانی مرورگر/OS موبایل، آدرس `http://lanshare.local:8420` هم کار کند (fallback به IP در صورت عدم پشتیبانی — پس QR اصلی و mDNS تکمیلی است).

---

## ۹. سیستم طراحی (UI/UX) — مینیمال، حرفه‌ای، بدون کلیشه

> این بخش برای جلوگیری از ظاهر "قالب عمومی SaaS" نوشته شده — رنگ‌ها، فونت و چیدمان دقیقاً مشخص شده تا خروجی حس ساخته‌شده و اختصاصی داشته باشد.

### پالت رنگی
| نام | Hex | مصرف |
|---|---|---|
| Paper (پس‌زمینه اصلی) | `#FAFAF7` | پس‌زمینه کل صفحه |
| Surface (سطح کارت‌ها) | `#FFFFFF` | کارت‌ها، پنل‌ها |
| Border | `#E4E2DB` | حاشیه‌ی ظریف بین بخش‌ها |
| Ink (متن اصلی) | `#20242A` | متن اصلی |
| Ink Muted (متن ثانویه) | `#6B7076` | توضیحات، متادیتا |
| Signal (تنها رنگ تاکیدی — حالت "روشن/متصل") | `#2F6F6B` (سبز-آبی تیره، حس شبکه/سیگنال) | دکمه toggle روشن، آیتم‌های فعال، لینک‌ها |
| Idle (حالت خاموش/غیرفعال) | `#C8C3B8` | toggle در حالت خاموش |

هیچ گرادینت، هیچ سایه‌ی نرم پیش‌فرض روی همه‌چیز، و هیچ رنگ دوم اضافه‌تر استفاده نشود. Signal تنها رنگ تاکیدی کل اپ است.

### تایپوگرافی
- فونت اصلی متن و عنوان‌ها: **Inter** (یک خانواده، دو وزن: 600 برای عناوین، 400 برای متن).
- فونت مونواسپیس فقط برای داده‌های فنی واقعی (آدرس IP، پورت، حجم فایل بایت): **JetBrains Mono**. این استفاده باید محدود به این موارد بماند، نه برای برچسب‌های عادی.
- بدون حروف بزرگ سراسری (ALL CAPS) برای لیبل‌ها. بدون eyebrow label بالای عنوان‌ها.

### چیدمان پنل دسکتاپ (المان اصلی و به‌یادماندنی: خود Toggle)
```
┌──────────────────────────────────┐
│                                   │
│         LAN Share                │   ← عنوان ساده، بدون لوگوی اضافه
│                                   │
│        ┌───────────┐             │
│        │   ◯──●     │            │   ← Toggle بزرگ و لمسی، المان اصلی صفحه
│        └───────────┘             │
│         در حال اشتراک             │  ← وضعیت فعلی، فقط وقتی روشنه
│                                   │
│   ┌───────────────────────────┐  │
│   │  [QR CODE]   192.168.1.23 │  │  ← فقط وقتی روشنه ظاهر می‌شه (fade+scale ساده)
│   │              :8420         │  │
│   └───────────────────────────┘  │
│                                   │
│   پوشه اشتراکی: ~/Shared  [تغییر] │
│                                   │
│   ─────────────────────────────  │
│   کلیپ‌بورد مشترک                 │
│   [ متن جدید... ]      [ارسال]   │
│   • «آدرس وای‌فای مهمان» — ۲ دقیقه پیش│
│   • «یادت نره نون بخری» — دیروز   │
└──────────────────────────────────┘
```
تنها انیمیشن قابل‌توجه: وقتی toggle روشن می‌شود، کارت QR با یک fade+scale ملایم (حدود ۲۰۰ میلی‌ثانیه) ظاهر می‌شود. بقیه‌ی اپ ساکن و بدون افکت است.

### چیدمان web-client (باز شده در مرورگر موبایل)
```
┌─────────────────────┐
│ 🏠 Shared / Photos    │  ← breadcrumb، قابل کلیک برای برگشت
├─────────────────────┤
│ 📁 Vacation           │
│ 📄 report.pdf   2.1MB │
│ 📄 notes.txt    4KB   │
├─────────────────────┤
│         کلیپ‌بورد ⌄    │  ← تب پایین صفحه، بین فایل‌ها و کلیپ‌بورد سوییچ می‌کنه
└─────────────────────┘
                    ⊕     ← دکمه شناور آپلود (پایین-راست)
```
- ردیف‌های فایل بدون کارت/سایه؛ فقط یک خط جداکننده‌ی ظریف (`border-b` با رنگ Border).
- در تب کلیپ‌بورد، ورودی متن بالای صفحه ثابت (sticky) با دکمه‌ی ارسال، و لیست پیام‌ها زیر آن (جدیدترین بالا).
- روی حالت خالی پوشه: پیام «این پوشه خالیه — یه فایل از پایین اضافه کن» (نه فقط "چیزی یافت نشد").

### اصول کلی طراحی (خلاصه)
1. یک المان جسورانه (Toggle بزرگ) — بقیه‌ی رابط کاملاً ساکت و منظم.
2. بدون کارت‌های یکسان با border-radius و سایه‌ی تکراری روی همه‌چیز (از الگوی «کیت کارت SaaS» پرهیز شود).
3. بدون em dash در لیبل‌ها، بدون فلش (→) در متن دکمه‌ها.
4. Responsive کامل تا موبایل، focus state قابل مشاهده برای کیبورد، رعایت `prefers-reduced-motion`.

---

## ۱۰. جریان‌های کاربری (User Flows)

### روشن‌کردن اشتراک‌گذاری
1. کاربر اپ دسکتاپ را باز می‌کند (یا از Tray icon باز می‌کند).
2. Toggle را می‌زند → `invoke('start_server')`.
3. Rust: پورت آزاد را چک می‌کند، axum را روی `0.0.0.0:PORT` بالا می‌آورد (چون فقط در شبکه‌ی محلی قابل‌دسترسی است)، mDNS را ثبت می‌کند.
4. `ServerStatus` برمی‌گردد → UI کارت QR و آدرس را نشان می‌دهد.

### اشتراک فایل از موبایل
1. کاربر QR را اسکن می‌کند → مرورگر موبایل صفحه‌ی `web-client` را باز می‌کند (خودِ `GET /`).
2. `GET /api/files?path=` صدا زده می‌شود → لیست ریشه نمایش داده می‌شود.
3. با دکمه‌ی شناور (+)، فایل موبایل انتخاب و `POST /api/files/upload` ارسال می‌شود.
4. سرور رویداد `file_added` را روی WebSocket به همه‌ی کلاینت‌ها (از جمله پنل دسکتاپ) broadcast می‌کند → لیست همه‌جا آپدیت می‌شود، بدون رفرش.

### ارسال متن به کلیپ‌بورد
1. از هر دیوایسی (دسکتاپ یا موبایل)، متن در باکس تایپ و «ارسال» زده می‌شود → `POST /api/clips`.
2. سرور در SQLite ذخیره می‌کند و `clip_added` را broadcast می‌کند.
3. روی دسکتاپ، کلیک روی آیتم → `invoke('copy_to_system_clipboard')` تا مستقیم در کلیپ‌بورد سیستم مینت کپی شود.

---

## ۱۱. امنیت (نسخه فعلی: بدون پسورد)

- سرور فقط روی رابط شبکه‌ی محلی گوش می‌دهد؛ IP بایند شده باید IP واقعی LAN باشد نه `0.0.0.0` بدون فیلتر (حداقل باید هدر `Host` و CORS محدود به origin های داخل همون subnet باشد — TODO پیاده‌سازی: whitelist ساده‌ی origin).
- بدون احراز هویت. **نکته‌ی معماری برای آینده**: تمام روت‌ها را می‌توان پشت یک middleware اختیاری قرار داد که در صورت وجود PIN در تنظیمات، هدر `X-Pin` را چک کند — این middleware باید از همین نسخه‌ی اول به‌صورت no-op (بدون‌عمل) در ساختار کد وجود داشته باشد تا افزودن بعدی فقط پیاده‌سازی منطق باشد، نه ریفکتور معماری.
- محدودیت حجم آپلود (مثلاً ۵۰۰ مگابایت هر فایل) در axum تنظیم شود تا از پرشدن دیسک جلوگیری شود.

---

## ۱۲. نصب و اجرا روی Linux Mint

### پیش‌نیازها
```bash
# Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# Node.js (برای build کردن Vue)
sudo apt install nodejs npm

# وابستگی‌های سیستمی Tauri روی دبیان/مینت
sudo apt install libwebkit2gtk-4.1-dev build-essential curl wget file \
  libxdo-dev libssl-dev libayatana-appindicator3-dev librsvg2-dev

# Tauri CLI
cargo install tauri-cli --version "^2.0.0"
```

### دستورات توسعه
```bash
# ساخت هر دو باندل Vue
cd desktop-panel && npm install && npm run build && cd ..
cd web-client && npm install && npm run build && cd ..

# اجرای حالت توسعه (دسکتاپ)
cargo tauri dev

# ساخت نسخه‌ی نهایی نصب‌شدنی (.deb / AppImage)
cargo tauri build
```

---

## ۱۳. نقشه‌ی راه پیاده‌سازی (فازبندی برای دستیار AI)

**فاز ۱ — هسته سرور**
- [ ] راه‌اندازی پروژه Tauri خالی + ساختار پوشه‌ها طبق بخش ۴
- [ ] پیاده‌سازی `commands.rs`: `start_server`, `stop_server`, `get_server_status`
- [ ] axum با یک روت تست (`GET /api/files`) که لیست یک پوشه‌ی هاردکد را برمی‌گرداند

**فاز ۲ — فایل‌ها**
- [ ] پیاده‌سازی کامل روت‌های فایل (لیست/آپلود/دانلود/mkdir/حذف) + بررسی امنیتی مسیر (بخش ۶)
- [ ] `web-client`: صفحه‌ی FilesView با breadcrumb و آپلود

**فاز ۳ — زنده‌سازی (WebSocket)**
- [ ] پیاده‌سازی `/ws` در axum + broadcast channel (`tokio::sync::broadcast`)
- [ ] اتصال هر دو باندل Vue به WebSocket و آپدیت خودکار لیست فایل‌ها

**فاز ۴ — کلیپ‌بورد**
- [ ] جدول SQLite برای ClipItem + روت‌های `/api/clips`
- [ ] کامپوننت‌های ClipboardPanel (دسکتاپ) و ClipboardView (موبایل)
- [ ] اتصال دکمه‌ی کپی دسکتاپ به `copy_to_system_clipboard`

**فاز ۵ — Discovery و پنل کنترل**
- [ ] mDNS broadcaster
- [ ] `ConnectionCard.vue` با QR Code + آدرس + کپی سریع آدرس
- [ ] `FolderPicker.vue` با دیالوگ بومی انتخاب پوشه

**فاز ۶ — پرداخت طراحی نهایی**
- [ ] پیاده‌سازی کامل سیستم طراحی بخش ۹ (رنگ، فونت، انیمیشن toggle)
- [ ] حالات خالی و خطا با متن راهنما (نه پیام فنی خام)
- [ ] تست روی حداقل یک موبایل اندروید واقعی در همان شبکه وای‌فای

---

## ۱۴. معیارهای پذیرش (Definition of Done)

- با یک کلیک روی toggle، سرور در کمتر از ۱ ثانیه بالا می‌آید و QR نمایش داده می‌شود.
- اسکن QR از موبایل اندروید (بدون نصب چیزی) مستقیم به فایل‌منیجر وب می‌رسد.
- آپلود یک فایل از موبایل، بدون رفرش، در پنل دسکتاپ ظاهر می‌شود (کمتر از ۲ ثانیه).
- ارسال متن از موبایل، همان لحظه در پنل دسکتاپ و هر موبایل دیگر متصل دیده می‌شود.
- خاموش‌کردن toggle، سرور را کامل می‌بندد و موبایل‌ها دیگر به آدرس دسترسی ندارند.
- خروجی build نهایی یک فایل نصبی (`.deb` یا `AppImage`) مستقل است که روی Linux Mint اجرا می‌شود.
